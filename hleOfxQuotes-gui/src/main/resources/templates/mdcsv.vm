## https://velocity.apache.org/engine/1.7/user-guide.html
## HEADER
$header
## ROWS
#foreach( $row in $rows )
#set( $symbol = $row.symbol )
#set( $price = $util.toPriceString($row.price, $language, $country) )
"$price","$symbol"
#end